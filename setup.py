from setuptools import setup
from paddecraftutilities.__init__ import VERSION

setup(
    name="paddecraftutilities",
    version=VERSION,
    description="A collection of utilities for PaddeCraft",
    author="PaddeCraft",
    packages=["paddecraftutilities"],
    install_requires=[],
    dependency_links=[],
)
